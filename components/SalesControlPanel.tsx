import React from 'react'

interface PanelProps { }

const SalesControlPanel = (props: PanelProps) => {
    return (
        <div>
            {/* Filters, sorters, searchers */}

        </div>
    )
}

export default SalesControlPanel